# Dependencies
node_modules/

# Build outputs (Vite / bundlers)
dist/
dist-ssr/

# Environment & secrets
.env
.env.*
!.env.example
apps/logdock/.env

# Deploy artifacts (podem embutir chaves do build)
.deploy-hub/

# OS & junk (macOS AppleDouble, Windows)
.DS_Store
._*
**/._*
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs & debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Caches & tooling
.cache
*.tsbuildinfo
# Vercel CLI — ignore local cache/tokens; keep linked project id for deploys
**/.vercel/**
!**/.vercel/project.json

# Scratch folders
**/scratch/

# Editor (optional — uncomment if you want repo-local settings ignored)
# .idea
# .vscode/*
# !.vscode/extensions.json
.vercel
.env*.local
.local-dev-logs/
